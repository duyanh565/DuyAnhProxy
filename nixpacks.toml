providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip3 install --break-system-packages -r requirements.txt"]

[start]
cmd = "python3 run.py"
