{
  "name": "jerry-proxy-server",
  "version": "1.0.0",
  "description": "Jerry SOCKS5 Proxy Server — xác thực bằng Key",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
